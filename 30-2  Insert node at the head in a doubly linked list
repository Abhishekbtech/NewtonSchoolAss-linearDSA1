public static Node insertnew(Node head,int k) {        
    Node newNode = new Node(k);

        if (head == null) {
            return newNode;
        }

        newNode.next = head;
        head.prev = newNode;

        return newNode;
}
